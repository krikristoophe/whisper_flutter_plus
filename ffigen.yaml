# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: WhisperFlutterBindings
description: |
  Bindings for `src/whisper_flutter.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/whisper_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/whisper_flutter.h'
  include-directives:
    - 'src/whisper_flutter.h'
comments:
  style: any
  length: full
